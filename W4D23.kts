//KOtlin test
var stackBottomPointer = 0
var stackTopPointer = 0
val stack : ArrayList<String> = arrayListOf()

class Node(value : Int){
    val key : Int
    var next : Node?

    init{
        key = value
    }
}

var head : Node?

for(i in 1..5){
    if(head.isNull())

}
fun 
fun insertIntoStack(){
    int temp = head
    while()
}